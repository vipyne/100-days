##### for use in .bash_profile / .bashrc

##### GIT STASH POOP #####

git()
{
  if [ "$1" == "stash" -a "$2" == "poop" ]
  then
      command git stash pop
      echo "
   )  )
  (   ) (
   ) _   )
    ( \_
  _(_\ \)__
 (____\___))"
  else
      command git "$@"
  fi;
}
