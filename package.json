{
  "name": "derpy",
  "version": "1.0.0",
  "scripts": {	
    "preinstall": "echo narf && npm run install:config",
    "install:config": "cd eslint && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
